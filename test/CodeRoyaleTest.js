var assert = require('assert');
const codeRoyale = require('../src/CodeRoyale');

describe('xxx', function() {
    describe('xxx', function() {
        it('xxx', function() {
            assert.equal(codeRoyale.xxx(), -1);
        });
    });
});